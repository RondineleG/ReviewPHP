<?php

$names = array("Jose", "felipe", "Rondinele");


// is_array();

echo is_array($names);
var_dump(is_array($names));
echo "<hr>";

if (is_array($names)) {
    echo " is array";
} else {
    echo " is not array";
}

echo "<hr>";

